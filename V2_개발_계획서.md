# MC_HideSeek V2 개발 계획서

## 0) 문서 목적
- 본 문서는 HideSeek 모드 V2를 **처음부터 재설계/재구현**할 때의 실행 기준 문서임.
- 목표는 기능 추가보다 먼저 **안정성, 구조 명확성, 운영 편의성** 확보임.
- 본 문서 기준으로 설계 검토, 구현, 테스트, 릴리즈 진행함.

---

## 1) 프로젝트 배경

### 1.1 현재 V1 상태 요약
- 기능은 풍부하지만 핵심 로직이 대형 서비스 클래스에 집중된 상태임.
- 설정/오디오/맵/직업/페이즈 흐름이 밀접 결합되어 변경 비용이 큰 상태임.
- 기능 확장은 가능하지만, 회귀 리스크와 디버깅 비용이 지속 증가하는 구조임.

### 1.2 V2 전환 필요성
- 맵 확장(다중 맵), 직업 확장, 사운드 확장 요구가 계속 증가 중임.
- 운영자가 컨픽만으로 조정 가능한 영역 확대 필요함.
- 클라이언트 연결 안정성(사운드 패킷/레지스트리 타이밍) 보장 필요함.

---

## 2) V2 비전과 성공 기준

### 2.1 비전
- “게임 규칙은 도메인 레이어에, 서버/Fabric 의존은 인프라 레이어에” 분리한 구조 달성 목표임.

### 2.2 핵심 성공 기준 (Success Metrics)
- 기능 동등성: V1 핵심 기능 100% 재현.
- 안정성: 라운드 100회 반복 soak 테스트에서 치명적 오류 0건.
- 유지보수성: 핵심 서비스 단일 파일 500줄 초과 금지(예외는 사유 기록).
- 운영성: 맵 추가/직업 조정/아이템명 변경이 코드 수정 없이 컨픽만으로 가능.

---

## 3) 범위 정의

### 3.1 포함 범위 (In-Scope)
- 라운드 페이즈 엔진(IDLE/COUNTDOWN/HIDING/COMBAT/ENDING).
- 팀/직업 시스템(능력, 쿨다운, 아이템 메타).
- 맵 시스템(구조물 로드, 마커 기반 랜덤 슬롯 변환).
- 사운드 시스템(코드 랜덤 선곡, 트랙 길이 기반 반복, 안전 fallback).
- 컨픽 시스템(전역 + 맵별 파일 분리, 버전/마이그레이션, 검증).
- 운영 명령어(시작/종료/리로드/디버그).

### 3.2 제외 범위 (Out-of-Scope)
- DB/외부 저장소 연동.
- 웹 대시보드.
- 클라이언트 모드 필수화.
- 월드 생성기(월드젠) 자체 개조.

---

## 4) 설계 원칙

### 4.1 코드 구조 원칙
- 단일 책임 원칙 준수.
- 페이즈 전이 규칙은 상태 머신 단일 엔진에서만 관리.
- 이벤트 핸들러는 라우팅만 담당, 실제 규칙은 유스케이스에서 처리.

### 4.2 운영 원칙
- 설정 파일은 읽기 시 무조건 rewrite 금지.
- sanitize로 값 수정이 발생하면 로그 남김.
- 잘못된 컨픽은 fail-fast + 명확한 운영 로그 출력.

### 4.3 안정성 원칙
- 사운드 재생 전 레지스트리 존재 확인 + fallback 사운드 강제.
- 능력 쿨다운/상태 초기화는 공통 유틸 메서드로 통일.
- 리스폰/재접속/관전 전환은 단일 정책 함수에서만 결정.

---

## 5) 아키텍처 설계

## 5.1 레이어 구성
- `domain`: 게임 규칙, 상태 전이, 승리 조건, 직업 정책(순수 로직)
- `application`: 유스케이스(라운드 시작/종료, 능력 사용, 맵 준비)
- `infrastructure`: Fabric 이벤트, 서버 API, 월드/엔티티 조작, 명령어 브리지
- `config`: 스키마, 파서, 검증기, 마이그레이터
- `presentation`(선택): 명령 응답 메시지, 플레이어 피드백 조립

### 5.2 권장 패키지 초안
```text
com.example.minigame.v2
  ├─ domain
  │   ├─ phase
  │   ├─ team
  │   ├─ job
  │   ├─ disguise
  │   └─ wincondition
  ├─ application
  │   ├─ round
  │   ├─ map
  │   ├─ ability
  │   └─ audio
  ├─ infrastructure
  │   ├─ fabric
  │   ├─ world
  │   ├─ command
  │   └─ audio
  ├─ config
  │   ├─ global
  │   ├─ map
  │   ├─ codec
  │   └─ migration
  └─ bootstrap
```

---

## 6) 도메인 모델 상세

### 6.1 핵심 Aggregate
- `GameSession`
  - sessionId, phase, phaseEndTick, participants, teamAssignments, roundStats
- `PlayerState`
  - role(team/job), disguiseState, abilityCooldowns, uiState
- `MapRuntimeState`
  - mapId, templateId, origin, size, markerSlots, activeSlots

### 6.2 상태 머신(Phase Engine)
- 전이 표(요약)
  - `IDLE -> COUNTDOWN`: 시작 명령/자동 시작
  - `COUNTDOWN -> HIDING`: 카운트다운 만료
  - `HIDING -> COMBAT`: 숨기 시간 만료
  - `COMBAT -> ENDING`: 승리 조건 만족 또는 시간 만료
  - `ENDING -> IDLE`: 결과 연출 종료

### 6.3 전이 가드 조건
- 필수 인원/팀 유효성 검사 통과.
- 월드/맵 로드 준비 완료.
- 컨픽 유효성 검사 통과.

---

## 7) 기능 설계 상세

### 7.1 맵 시스템

#### 7.1.1 요구사항
- 맵별 JSON 분리 관리.
- 구조물 템플릿 기반 맵 원복.
- 고정 origin + 고정 게임공간 크기 컨픽화.
- 마커 블록 스캔 후 active_count만큼 슬롯 활성화.

#### 7.1.2 데이터 모델
- 전역 `hide_seek.json`
  - `map_origin`, `game_space_size`, `slot_randomization`, `defaults.disguise_blocks`
- 맵별 `maps/*.json`
  - `id`, `structure_template`, `disguise_blocks`(선택)

#### 7.1.3 슬롯 처리 규칙
- 마커 변환은 `active_count` 중심.
- `weight`는 **블록팀 위장 배정 확률** 전용.
- 마커 변환 로직에서 `weight` 사용 금지.

### 7.2 직업/능력 시스템

#### 7.2.1 공통 프레임워크
- `AbilityExecutor` 인터페이스 통일.
- 트리거 아이템/쿨다운/효과 사운드/파티클 표준화.
- 페이즈별 사용 가능 조건(예: HIDING 중 술래 능력 차단) 공통 검사.

#### 7.2.2 사냥꾼 도약 예시 규칙
- 우클릭 트리거: FEATHER.
- 도약 벡터 계산: 시선 기반 + 상향 보정.
- 피드백: 박쥐 takeoff + cloud 파티클.
- 쿨다운/아이템명/로어는 job config에서 조정.

### 7.3 사운드 시스템

#### 7.3.1 원칙
- 리소스팩 랜덤 사용 금지.
- 모드 코드에서 트랙 랜덤 선택.
- 트랙 길이 기반 반복 예약.

#### 7.3.2 안전 장치
- 재생 전 `Registries.SOUND_EVENT` 존재 검사.
- 미등록 시 fallback 이벤트 재생.
- 정지 시 stopsound ID 일관 사용.

### 7.4 페이즈/보스바/UI
- 보스바는 페이즈 전이와 완전 동기화.
- 사망/리스폰/관전 전환 시 보스바 누락 방지.
- 액션바/타이틀 메시지는 텍스트 컨픽 키 기반 렌더링.

### 7.5 접속/리스폰 정책
- 게임 중 신규 접속자는 기본 관전.
- 단, 동일 라운드 참가자 재접속은 관전 예외.
- 질식 피해(IN_WALL) 차단 유지.

---

## 8) 컨픽/마이그레이션 전략

### 8.1 스키마 버전 정책
- 모든 JSON에 `version` 필드 유지.
- 버전별 마이그레이터 체인 제공(`v1 -> v2 -> v3`).

### 8.2 로드/검증 흐름
1. JSON 읽기
2. 스키마 버전 확인
3. 필요 시 마이그레이션
4. sanitize
5. validation
6. 런타임 모델 생성

### 8.3 파일 쓰기 정책
- 기본: read-only 로드.
- 자동 쓰기는 아래 경우만 허용
  - 파일 최초 생성
  - 운영자가 `normalize` 명령 실행
  - 치명적 필드 누락 복구가 필요한 경우(명시 로그 필수)

---

## 9) 테스트 전략

### 9.1 단위 테스트 (Unit)
- 상태 머신 전이 테스트.
- 승리 조건/시간 종료 판정 테스트.
- 능력 쿨다운 계산/초기화 테스트.
- 맵 슬롯 샘플링(active_count, remove_state, marker 매칭) 테스트.

### 9.2 통합 테스트 (Integration)
- 라운드 시작~종료 전체 플로우.
- 재접속/리스폰/관전 전환 시나리오.
- 컨픽 리로드 시 런타임 상태 유지/초기화 검증.
- 사운드 재생 이벤트 등록/정지 경로 검증.

### 9.3 E2E 스모크
- 서버 기동 -> 플레이어 접속 -> 게임 시작 -> 종료.
- 맵 구조물 로드 + 마커 변환 적용 확인.

### 9.4 품질 게이트
- CI 기본: `clean build` 필수.
- 릴리즈 전: 30회 라운드 반복 soak.
- 크래시/연결끊김 0건일 때만 RC 통과.

---

## 10) 성능/안정성 계획

### 10.1 성능 포인트
- 맵 스캔 범위는 고정 박스(63x63x32) 기준 최적화.
- 대량 블록 치환은 필요 시 틱 분할 배치 옵션 제공.
- 불필요한 객체 할당 최소화(핫패스 재사용).

### 10.2 안정성 포인트
- 각 페이즈 진입 시 공통 reset 훅 실행.
- 능력 map clear 중복 제거(공통 메서드).
- 플레이어 상태 cleanup 표준화.

---

## 11) 개발 일정(8주 기준)

### Week 1: 스펙 고정/아키텍처 확정
- V1 기능 parity 체크리스트 확정.
- 패키지 구조, 상태 머신 인터페이스 확정.

### Week 2: 코어 엔진
- GameSession, PhaseEngine, TransitionGuard 구현.
- 단위 테스트 1차 구성.

### Week 3: 맵 시스템
- 맵 로더/마커 스캔/slot randomization 구현.
- 맵별 disguise 목록 + weight 배정 규칙 구현.

### Week 4: 능력 프레임워크
- AbilityExecutor 파이프라인.
- 직업별 능력 이식(봄버/워든/사냥꾼/기타).

### Week 5: 오디오/UI
- 오디오 카탈로그/스케줄러/fallback 체계.
- 페이즈 경고/보스바/액션바 동기화.

### Week 6: 운영 기능/리로드
- admin 명령/디버그 명령.
- 컨픽 리로드 안정화.

### Week 7: 통합 테스트/하드닝
- 재접속/리스폰/관전/사망 케이스 집중 검증.
- soak 테스트 + 로그 튜닝.

### Week 8: 릴리즈 후보
- RC 빌드, 릴리즈 노트, 운영 가이드 작성.

---

## 12) 산출물(Deliverables)
- `V2_ARCHITECTURE.md`
- `V2_CONFIG_SCHEMA.md`
- `V2_STATE_MACHINE.md`
- `V2_TEST_PLAN.md`
- `V2_OPERATIONS_RUNBOOK.md`
- `CHANGELOG_V2.md`

---

## 13) 리스크 레지스터

### R1. 기능 누락
- 설명: V1 대비 기능 parity 누락 위험.
- 대응: 체크리스트 기반 수동/자동 검증.

### R2. 사운드 연결 안정성
- 설명: 커스텀 사운드 레지스트리 타이밍 이슈.
- 대응: 재생 전 레지스트리 확인 + fallback + 진단 로그.

### R3. 설정 호환성
- 설명: 기존 컨픽 호환 깨짐 위험.
- 대응: 마이그레이션 어댑터 + 버전 필드 강제.

### R4. 성능 저하
- 설명: 맵 스캔/블록 변환 부하.
- 대응: 범위 최적화 + 분할 배치 옵션 + 프로파일링.

---

## 14) Definition of Done (DoD)
- 코드 리뷰 통과.
- 단위/통합 테스트 통과.
- `clean build` 통과.
- 테스트 서버 기동 성공.
- 관련 문서 업데이트 완료.
- 운영자가 컨픽만으로 해당 기능 조정 가능 확인.

---

## 15) 구현 착수 전 체크리스트
- [ ] V1 parity 목록 합의 완료
- [ ] 패키지 구조/네이밍 합의 완료
- [ ] 상태 머신 전이표 합의 완료
- [ ] 컨픽 스키마 초안 합의 완료
- [ ] 테스트 전략/품질 게이트 합의 완료
- [ ] 릴리즈 기준(성능/안정성) 합의 완료

